# 修改日志 · CHANGELOG

> 记录站点（Hjy2014.github.io）的每一次功能新增与 Bug 修复。
> 规则：**最新的在最上面**；每个日期内 **功能在上、Bug 修复在下**。
> 每次让我改动东西，这里都会同步更新并发给你。

---

## 2026-09-22 · 第 11 批（手机端音乐区：收藏/历史恢复原样 + 每日推荐两列收小并修横向溢出）

### 🔄 调整（手机端）

1. **「我的收藏 / 历史」恢复原来的样子**：各自占一整行（上次为了跟电脑端统一把它们挤到了一行）；搜索栏保持上次的改动——输入框和「搜索」按钮同一行。
2. **每日推荐两列整体收小**：卡片内边距、图标、字号都缩小一档。

### 🐛 Bug 修复

1. **长歌手名把每日推荐撑出屏幕**：两列写法用了 `1fr`，列宽不能小于卡片内容宽度，多歌手的长名字一行不换行，直接把整页撑到横向能滑（实测 360px 机型页面宽被撑到 556px）。→ 改成 `minmax(0, 1fr)` 强制列宽相等且可收缩，歌手名超出部分自动省略号截断。360px 复测页面宽度恢复正常。

---

## 2026-09-22 · 第 10 批（修复：主页底部「浏览设备数」不见了）

### 🐛 Bug 修复

1. **主页底部「👀 本站已被 N 台不同设备浏览过」时不时就没了**：两个原因叠加——① 计数接口请求只要失败一次（第三方计数服务偶发抽风/被网络拦），代码就把计数元素整个删掉，之后再也不会显示；② 站内换页是无刷新的，回主页时底部是全新的空元素，但代码里有个「只初始化一次」的守卫直接跳过，永远不填。
   → 重写：**数值拿到后记在页面常驻层**，换页回主页立刻回填新元素；接口失败**不再删元素**，改为 3 秒 / 6 秒 / 9 秒退避重试最多 3 次；防刷逻辑（无头/爬虫不计、新设备等首次真实交互）保持不变。

---

## 2026-09-22 · 第 9 批（修复：游戏全部提示「页面已崩溃」）

### 🐛 Bug 修复

1. **所有游戏打开都报「呀，出错了。抱歉，页面已经崩溃」**：游戏文件是由 turbowarp.org 的播放器 iframe 去下载的，你的网络到 GitHub Pages（hjy2014.github.io）的请求一被掐断，iframe 里只会显示「TypeError: Failed to fetch」的崩溃页，看起来像网站坏了。实测网站和全部游戏文件本身都是好的。
   → 改成**两段式加载**：点开游戏后先由我们自己的页面把游戏文件下载好（哪条路通就走哪条），成功后再交给播放器；GitHub 原地址不通时自动切 **jsDelivr CDN 镜像**（国内可达性好，20MB 以内文件都支持）；两条路都断时显示明确的「📡 加载失败 + 🔄 重试」按钮，不再是一脸懵的崩溃页。下载好的文件会预热缓存，播放器秒开。

---

## 2026-09-22 · 第 8 批（下线「登录记录」功能）

### 🗑 移除（电脑 + 手机）

1. **登录记录功能整体下线**：站长菜单里的「📋 登录记录」入口、登录记录弹窗、以及后台的登录流水记录（含对应样式与本地存储）全部删除，登录 / 退出 / 管理网页 / 更新日志等其他功能不受影响。

---

## 2026-09-22 · 第 7 批（方块火线：开火规则收紧 + 鼠标不再隐藏 + 灵敏度设置）

### ✨ 功能（电脑 + 手机）

1. **开火只认开火键**：电脑端只有**按住鼠标左键**才开火（点按钮、点面板、点返回键都不会误开枪）；手机端只有**按住开火键**才开火。切换窗口 / 切后台时自动松开，杜绝「回来后枪一直在响」。
2. **鼠标不再隐藏，直接控制视角**：去掉了原来的「点击锁定鼠标」机制（Pointer Lock），对局中鼠标指针全程可见，**移动鼠标直接转动视角**，不用先点一下锁定；**按 ESC 弹出暂停菜单**。
3. **所有界面都显示鼠标**：对局、游戏内大厅、阵亡倒计时页、结算、设置，鼠标指针一律可见，不再有任何「找不到鼠标」的时刻。
4. **全屏自动横屏**：手机上点全屏（⛶）后会尝试锁定横屏；万一系统不让锁而你又是竖屏，会弹出「📱 横过来玩更爽」的提示页，转过来就能继续玩。
5. **新增设置面板**：大厅「⚙ 设置（灵敏度 / 音量）」和暂停菜单「⚙ 设置」都能打开，三条滑杆——**鼠标灵敏度**（0.2~3.0）、**触屏灵敏度**（0.5~3.0）、**音效音量**（0~100）。滑杆**即时生效、自动保存**，下次进游戏不用再调。

### 🐛 Bug 修复

1. **改完设置游戏直接白屏**：第一版把横屏提示的监听器注册在了监听管理器初始化之前，页面一加载就报 `Cannot read properties of undefined`，游戏整个起不来。已把注册挪到初始化之后，回归测试全绿。

### ✅ 验证

- CDP 无头浏览器实测 `ALL_OK`（原 36 项 + 新增 8 项）：设置面板打开 → 拖灵敏度滑杆到 2.5 → `localStorage` 保存成功、读回一致 → 关闭后回到大厅 ✓；对局中 `cursor` 为 `auto`（不隐藏）✓；模拟鼠标右移 100px → 视角 yaw 0 → -1.089（向右转，灵敏度生效）✓；按 ESC 弹暂停菜单、鼠标可见 ✓；阵亡页鼠标可见 ✓；开火 / 记分板 / 重生 / 胜负 / 计时等原有 36 项全部回归通过，全程 0 JS 报错 ✓。

---

## 2026-09-21 · 第 6 批（新游戏「方块火线」上线，替换极简 Markdown 编辑器）

> 🚧 本批游戏目前**先做在本地**，等你下令再正式上线；上线后主页「极简 Markdown 编辑器」卡片会替换成它。

### ✨ 功能（电脑 + 手机）

1. **全新 3D 方块射击游戏「方块火线」**：受 Poki 上 Blockpost 启发，用 three.js 从零写的浏览器 FPS，主页入口换掉「极简 Markdown 编辑器」。对局规则和原版一样——**5 个机器人对手 · 先杀敌到 20 获胜 · 10 分钟限时**，死了 3 秒复活，杀人涨经验。
2. **游戏内大厅**：进游戏先站在大厅里，自己的 MC 皮肤方块人立在场景中，移动鼠标环视角色；选「休闲 / 竞技 / 硬核」三种匹配难度（数字键 1/2/3），空格或点按钮开始匹配。大厅里鼠标指针自动隐藏，只用来转视角。
3. **选枪界面**：点开始匹配后先选武器——手枪 / 步枪 / 霰弹枪 / 随机（数字键 1~4 或点击卡片），选完立刻空降战场。
4. **Blockpost 风对局 HUD**：
   - 左上角 **Lv.1 经验条 + 全员记分板**（你 + 5 个机器人，击杀数实时排序，先到 20 结算）；
   - 顶部 **10:00 倒计时**和血量百分比；
   - 击杀 +100 经验、爆头 +150，**满经验自动升级**（Lv.2、Lv.3……升级弹横幅）；
   - 命中敌人**飘伤害数字**（爆头飘黄色带感叹号），击杀后在准星下方飘「+100 XP」；
   - **敌人头顶红色 ID**，穿墙也能看到人在哪（和原版一样）；
   - 击杀播报（谁击倒了谁）在右上角滚动。
5. **机器人更像真人对手**：每人有随机游戏 ID（蹲坑小能手、AK小王子、老六本六……）、随机 MC 风皮肤，会追人、绕掩体、卡视野突进，死后 2 秒在远离你的出生点重生，击杀数也计入记分板——被打死会看到对面涨分，先到 20 对面赢。
6. **低多边形地图「货柜场」**：卡其色地面、棕红大围墙、饱和色集装箱掩体、150 丛锥形小草、天上飘方块云，纯色低多边形风（和录屏里的 Blockpost 一个味儿）。
7. **玩家自己的 MC 皮肤方块人**：第三人称能看到（Steve 风格皮肤：青色上衣、蓝裤子，程序逐像素绘制）；走动时手脚摆动。
8. **7 种枪械细节**（第一人称视角模型）：手枪有套筒/准星/照门，步枪有机匣/消焰器/提把，霰弹枪是木质枪身 + 泵动护木；换枪、装填都有动作和音效（WebAudio 合成，无外部资源）。

### 🐛 Bug 修复

1. **对局 HUD 记分板和经验条文字重叠**：记分板面板没有定位样式，被绝对定位的经验条压在同一行。已给记分板独立定位（电脑端经验条下方，手机端同步调整），不再叠字。
2. **旧版「波次」残留代码引用已删除的元素**会报错：改匹配制时把引用旧 HUD 元素的计分函数整个重写，全流程零报错。

### ✅ 验证

- CDP 无头浏览器实测 `ALL_OK`（36 项断言）：大厅方块人可见 → 开始匹配进选枪（4 张枪卡）→ 按数字键选枪入局 → 记分板 6 行全员在列、敌人红名全亮 → 计时器正常倒数 → 开枪出伤害飘字、击杀 +150 XP、击杀播报带 XP → 死掉的 bot 2.6 秒后重生（存活回到 5）→ 玩家被打倒进 3 秒重生倒计时、复活满血 → 击杀到 20 弹「🏆 胜利！」结算（击杀/等级/命中率/爆头）→ 再来一局回到选枪 → 时间到弹「⏰ 时间到」→ 返回大厅，全程 0 个 JS 报错 ✓。
- 三张截图（选枪界面 / 对局画面 / 胜利结算）人工核对通过：低多边形世界、锥形草丛、左上记分板+经验条、穿墙红名、HUD 布局与录屏里的 Blockpost 对齐。

---

## 2026-09-21 · 第 5 批（计数器校准 + 测试浏览永久免计）

### ✨ 功能

1. **访问计数校准为 10**：之前换的防刷计数器创建时没拿到管理密钥，数值改不了（当前是 3）。这次用 `/create` 新开了一个带管理密钥的计数器（`hjy2014io/devices-v2`），**初始值直接设为 10**；管理密钥已本地留存，以后想调数值随时可调（`/set` 接口）。
2. **AI / 测试浏览永久不计入**：站点代码新增两道拦截——无头浏览器（UA 含 `Headless`）打开页面直接跳过计数流程；带 `?e2e=1` 测试参数打开的页面同样跳过。以后无头回归测试、E2E 验证都刷不动这个数字。

### 🐛 Bug 修复

1. **自动化检测有漏网**：之前只查 `navigator.webdriver`，实测这台机器的无头 Edge 报的是 `false`（拦截形同虚设，防刷主要靠「等真实交互」兜底）；这次补上 UA 指纹检测，`webdriver` + `Headless UA` + `e2e 参数` 三层拦截。

### ✅ 验证

- CDP 无头浏览器实测 `ALL_OK`：带 `?e2e=1` 加载主页 → abacus 请求 0 次、计数条已移除 ✓；不带 e2e 直接加载（无头 UA=`HeadlessChrome/153`）→ abacus 请求 0 次、计数条已移除 ✓。
- 线上新计数器 `GET /get/hjy2014io/devices-v2` → `{"value":10}` ✓。

---

## 2026-09-21 · 第 4 批（访问计数防刷 + 站长登录记录）

### ✨ 功能

1. **站长专看「📋 登录记录」**：用站长账号（hjy2014）登录后，头像菜单在「📝 网站更新日志」和「退出登录」之间多了一项「📋 登录记录」，点开弹窗展示这台设备上每次登录的**时间、账号、设备类型（电脑/手机）**，最新的排最前，最多留 200 条。普通账号看不到这个入口。说明：本站是纯静态网页（GitHub Pages），没有服务器，**只能记录本机的登录**，其他设备上的登录收集不到——弹窗里也写明了这一点。
2. **访问计数防刷升级**：首页「👀 本站已被 N 台不同设备浏览过」换用全新计数器从零开始重计。老计数器已经被爬虫、无头测试刷到 48，且没法改数值。新逻辑：
   - **新设备要等首次真实交互**（点击 / 按键 / 触摸）才 +1 —— 只加载页面不互动的爬虫、扫描器、链接预览统统不计；
   - 自动化浏览器（`navigator.webdriver`）直接跳过计数；
   - 同一次浏览只走一遍流程（防 pjax 反复触发）。

### 🐛 Bug 修复

1. **登录记录弹窗没有遮罩样式**：弹窗遮罩样式原本只挂在 `#loginModal` 一个 id 上，新加的 `#loginsModal` 啥都没吃到，白卡片掉到页面最底部看不见。已让两个弹窗共用同一份遮罩样式（暗色毛玻璃、居中）。

### ✅ 验证

- CDP 无头浏览器实测 `ALL_OK`：加载后 0 次计数请求 → 模拟真实按键后恰好 1 次 `/hit/hjy2014io/devices` → 再次手势不重复计数 ✓；计数文本与防重标记（`hjy_visited_v2`）正确写入 ✓；站长登录后菜单顺序 = 更新日志 < 登录记录 < 退出登录 ✓；登录记录流水（多账号 hjy2014 / 小明 / hjy2014，新的在前、设备类型「电脑」）✓；全程 0 个 JS 报错 ✓。

---

## 2026-09-21 · 第 3 批（登录菜单加更新日志入口）

### ✨ 功能

1. **登录后菜单新增「📝 网站更新日志」**：点开直达日志页。
2. **菜单顺序调整**：🛠 管理网页 → 📝 网站更新日志 → 退出登录，**「退出登录」挪到最底下**，更新日志排第二位（普通登录用户排第一位）。
3. **日志页支持站内无刷新切换**：changelog.html 改造为站点壳页面（样式移入 body 并全部作用域隔离到 `.log-page`，不会污染导航栏 / 播放器），从菜单点进去**音乐不会中断**，点返回键直接回到登录后的主页。
4. changelog.html 引入站点样式表，独立打开和站内切换两种方式外观一致。
5. **删掉日志页顶部的说明条**（「最新的在最上面；每个日期内功能在上、Bug 修复在下……」），页面更干净，标题下面直接就是日志卡片。
6. **返回键从页面最底下挪到左上角，改成圆形左箭头**：原来是页尾的一行文字链接「← 返回主页」，得滚到底才能点；现在是 44×44 的正圆按钮（白底、浅描边、箭头用站内主色紫，悬停变浅紫底、按下轻微缩放，和站内其它按钮风格统一）。电脑宽屏（≥900px）**固定在左上角**，往下翻日志时也随时能点；手机窄屏排在标题上方，不会压到正文。

### ✅ 验证

- CDP 无头浏览器实测（真实鼠标手势）：菜单三项顺序 = 🛠 管理网页 / 📝 网站更新日志 / 退出登录 ✓；点日志项 263ms 完成切换、`<audio>` 节点为同一实例（音乐没断）✓；日志页 h1 30.4px、卡片白底、样式生效 ✓；返回主页后登录态（hjy2014 站长）保持、菜单入口仍在 ✓。
- 返回键实测：圆形（44×44、`border-radius: 50%`）✓、位于页面左上角（不再在页尾）✓、点击后无刷新回到主页且音乐不断 ✓。

---

## 2026-09-21 · 第 2 批（手机端体验 6 连改）

### ✨ 功能

1. **游戏弹窗提示语精简**：删掉了「玩的时候会自动帮你缓冲后面 3 个游戏」这句话（预缓冲功能本身还在，只是不再写出来）。
2. **游戏搜索框占位语精简**：删掉了「（比如：玛丽、坦克、僵尸）」，只留「输入游戏名字，一键搜索」。
3. **游戏弹窗删除原生的方形放大按钮**：TurboWarp 内嵌播放器自带的 ⛶ 全屏按钮不再显示（官方文档确认：iframe 去掉 `allowfullscreen` 属性即隐藏该按钮），只保留弹窗头部的圆形全屏键（− □ ✕ 那颗 □），功能不受影响。
4. **手机端悬浮播放窗初始大小**：改为约 2/3 屏宽（实测 412px 视口下宽 272px），与你截图里的大小一致。之前手动缩放过的话会记住你调过的尺寸，不受影响。
5. **手机端音乐搜索栏与电脑端一致**：「搜索」按钮回到输入框右侧；「我的收藏 / 历史」放不下时自动排到下一行，不再各占一整行。
6. **手机端每日推荐改为两列**：不再一列一张大卡片，一屏能看到更多歌。
7. **手机端「选择」键变圆形**：历史记录的「选择」按钮空闲时是正圆（48×48）；进入选择模式后因为文字变长（🗑 清理 / 取消选择），自动恢复胶囊形以免撑破。
8. **新增本「修改日志」页面**（changelog.html / CHANGELOG.md），以后每次改动都会更新并附上。

### ✅ 验证

- 手机视口（412px）无头浏览器实测：搜索键与输入框同行 ✓、收藏/历史换行 ✓、网格 2 列 ✓、提示语与占位语干净 ✓、iframe 无 allowfullscreen 且圆形全屏键在位 ✓、悬浮窗布局宽 272px ✓（首帧 250px 是入场动画 scale 0.92 被量到，动画结束即 272）。

---

## 2026-09-21 · 第 1 批（游戏库扩容 + 封面高清化）

### ✨ 功能

- **游戏库 1717 → 1866 个**：剩余 149 个大文件（约 1.1GB）全部上架。
  - 弹钢琴（28.10MB）：zip 无损重打包为 23.67MB 后经 API 上传（内部 63 个资源逐字节一致）。
  - 超级粉碎（31.69MB）：放行 github.com 后走 **git push 二进制通道原样上传**，线上 33225839 字节与原文件完全一致。
- **放大后的歌曲封面清晰化**：详情页大封面改渐进加载（先显 240 缩略图、500×500 高清无缝替换）；锁屏/系统媒体控件封面升到 500×500；歌手头像按 300 取图。
- 站内每张游戏卡片保持预取后 3 个游戏（老功能，继续生效）。

### 🐛 Bug 修复

- **封面高清从未生效**：`bigArt()` 原来只对「不带参数」的 URL 追加高清参数，而封面 URL 都带着 `?param=240y240` → 改为原地替换 `param=`。
- **网络设备伪装 401/201/404**：中间设备对超过约 34MB 的请求体拦截并伪装成 `401 Bad credentials`，还会伪造 `201`（blob 实际没落地）和 `404` → 上传后一律校验「返回 sha == git hash-object」。
- **Actions 服务端拼接方案不可行**：token 缺 `workflow` scope，tree 路径含 `.github/workflows/` 直接 404（已隔离验证，封档）。
- **Pages 构建延迟的伪 404**：超级粉碎推完后线上短暂 404，实为 hjy-games-2 Pages 还在构建（1~3 分钟），构建完成即 200。
- 提交信息里的中文被本机编码转坏 → 提交信息改用纯 ASCII。

---

## 2026-09-20（音乐模块大改版）

### ✨ 功能

- **音乐跨页面不断播 + 浏览器最小化持续播放**：
  - `js/site.js` 站点壳：站内跳转只换页面内容、不销毁播放器（`#pageRoot` + 常驻 `#pfRoot`），前进/后退走 pushState/popstate。
  - `js/player.js` 常驻播放器：`<audio>` 永不重建；接入 Media Session（锁屏/媒体键可控）；播放现场持久化（`hjy_player_state_v1`，刷新也能接着播）。
  - **歌词只换驱动方式**：`timeupdate` + 400ms 定时器双驱动（后台标签页 timeupdate 会变稀），显示效果完全没变。
- **点击歌手名弹出歌手页**：卡片上的歌手名可点（外观与纯文本一致，hover 才变色）→ 头像 + 单曲/专辑数 + 简介 + 热门 50 首 + 「▶ 全部播放」（先过滤无版权歌曲并提示可播数量）；资料三接口并行 + 悬停预取。
- **换页显示记忆**：主页上次那一屏（每日/搜索/收藏/榜单）与游戏清单挂 window 缓存（`__hjyHomeMemo` / `__hjyGamesCache`），回主页 186ms 出卡片、二次往返 10ms。
- 历史记录多选删除、清空确认弹窗（Windows 风格控制键：− 最小化 / □ 全屏 / ✕ 关闭）。

### 🐛 Bug 修复

- **site.js boot() 死循环**：`while (firstChild)` 只 push 不摘除，RangeError → 改为先快照子节点再搬移，并加 try/catch 兜底。
- **index.html 的 `data-page="home"` 丢失**：同文件并行编辑互相覆盖导致页面脚本不执行（音乐卡片 0 个）→ 教训入档：**同一文件的多处修改必须串行**。
- **弱网下写入空每日缓存**：`if (r.checked)` 改为 `if (r.checked && r.list.length)`。
- **歌手弹窗「全部播放」文案未复位**：加载状态结束时重置按钮文字。
- **无刷新换页后模块级缓存全部重置**：回主页空白几十秒重新走弱网请求 → 缓存挂 window。
- **部署脚本对新文件 404 重试 4 次白等 60s** → 404 视为「无 sha」直接继续。

---

## 更早（2026-09-19 及之前）

- 站点基础：作品集 + 音乐板块 + 游戏库三页，GitHub Pages 部署（api.github.com + Windows 凭据管理器 token）。
- 游戏库三仓库分片架构（主站 + hjy-games-2 + hjy-games-3），269 个大文件计划。
- 音乐模块对接网易云 API（搜索/每日推荐/收藏/历史/分页/歌词），过滤无版权/VIP 歌曲。
- 悬浮播放窗：可拖动、可缩放、位置与缩放持久化；弹窗控制键 Windows 风格。
